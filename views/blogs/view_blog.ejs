<!DOCTYPE html>
<html lang="en">
  
<head>
  <title>Paraverse - <%= title %></title>
  <%- include('../partials/_head.ejs') %>
</head>

<body>
  <div>
    <!-- TOP NAVIGATION BAR -->
    <%- include('../partials/_navbar.ejs') %>
    <br><br><br><br><br>
    <!-- HEADER -->
    <div class="welcome">
      <h2>Paraverse  Blog</h2>
      <section>Stay up to date with Paraverse Studio. View all our latest blogs here.</section><br>
      <hr class="welcome"><br>
    </div>
    
    <section class="blogs-section">
      <div class="view-blog-btns-container">
        <a href="/blogs" class="a-btn articles-btn">All Articles</a>
        <% if (user != null || user != undefined) { %> 
        <a href="/blogs/edit/<%= blog.id %>" class="a-btn edit-btn">Edit</a>
        <% } %> 
      </div>
      <h4 class="article-title"><%= blog.title %></h4>
      <div class="blog-date-text">
        <%= blog.createdAt.toLocaleDateString() %>
      </div>
      <span>
        <% if (blog.author === "Abhinav Mohan") { %>
          <img class="author-image" src="../../images/Abhinav.jpg"><span class="author-name"><%= blog.author %></span>
        <% } else if (blog.author === "Abhishek Mohan") { %>
          <img class="author-image" src="../../images/Abhishek.jpg"><span class="author-name"><%= blog.author %></span>
        <% } else if (blog.author === "Prabdeep Dhaliwal") { %>
          <img class="author-image" src="../../images/Prabdeep.png"><span class="author-name"><%= blog.author %></span>
        <% } else { %>
          Author not named
        <% } %>
      </span>
      <div class="article-container">
        <%- blog.sanitizedHtml %>
      </div>
    </section>
    
    <!-- FOOTER  -->
    <%- include('../partials/_footer.ejs') %>
  </div>
</body>
</html>